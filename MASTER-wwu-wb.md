# MASTER — WWU Withdrawal Button

> Single index for the **WWU Withdrawal Button** plugin: the EU online right-of-withdrawal function ("withdrawal button", Art. 11a / Art. 54-bis) for WooCommerce & FluentCart. One line per doc; never put content here.

- **Slug:** `wwu-wb` · **Folder:** `wwu-withdrawal-button`
- **Status:** **MVP feature-complete** (F0–F8 + audit hardening), merged to `main`, **released** [`v1.0.0-alpha.40`](https://github.com/An-Idea-For-Business/wwu-withdrawal-button/releases/tag/v1.0.0-alpha.40) — in live testing. Current build `1.2.5` (**`1.2.5` PHP 7.4 fix + multi-recipient alerts** — issue #31: bundled Dompdf was `^3.1` (v3.1.5) whose tree (`sabberworm/php-css-parser` 9.x + `thecodingmachine/safe`) requires PHP 8.1 and 3.x uses 8.1 syntax, contradicting `Requires PHP 7.4` → Composer's `platform_check.php` threw *"require a PHP version >= 8.1.0"* on 7.4 near the PDF setting. Pinned `dompdf/dompdf` to `^2.0` (v2.0.8, tree 7.1+); `composer.lock` regenerated, `vendor/` not committed (built from lock at package time), standalone render re-verified valid `%PDF`. Plus: `merchant_email` setting now accepts a comma-separated list (`Sanitizer::email_list()` = sanitize_email each + dedupe + cap 10) for multi-recipient "new request" alerts — `wp_mail` takes the comma `to` natively; the consumer receipt shows only the FIRST address as the public trader contact (`Sanitizer::first_email()`). 3 smoke assertions added. **Released `v1.2.5` to GitHub.** Prior **`1.2.4` WordPress.org pre-review hardening** — addressed the wp.org plugin-directory pre-review (Review ID `TRM-OWN-LIC`): ownership verified by moving the WP.org account to `@webwakeup.it` (WWU = WebWakeUp, our own brand, not a third party's mark); display name refined "WWU Withdrawal Button" → **"WWU Right of Withdrawal"** keeping the `wwu-withdrawal-button` slug + text domain (zero i18n churn); `sanitize_text_field` on `REMOTE_ADDR` in `GuestAccess`, `esc_url` (not `esc_url_raw`) in the 3 plain-text-email URL outputs, explicit `permission_callback => '__return_true'` inlined on the public `WithdrawalRoute` endpoints, removed the unneeded `load_plugin_textdomain`, readme External-services expanded for the opt-in RFC 3161 authority. False positives explained to the reviewer (no trialware, inline `<style>` only in standalone HTML/PDF, intentionally-public REST). **Released `v1.2.4` to GitHub** + zip for wp.org resubmission. Prior **`1.2.3` detailed mail-failure reason** — a failed acknowledgement e-mail now reports the mail transport's EXACT reason in the admin notice + immutable log instead of a generic "email failed": `Mailer::send_html()` captures it via new `Mailer::last_error()` (the `wp_mail_failed` `WP_Error` when `wp_mail()` returns false, or the thrown message when it throws; trimmed + 300-char capped), `ConfirmationDispatcher` threads it into the `receipt_failed` log payload + the `wwu_wb_mail_failed` transient (now `array{uid,reason}`), `AdminController` appends "Reported reason: …" to the notice — an SMTP misconfig (WP Mail SMTP/FluentSMTP) is diagnosable at a glance. **Released `v1.2.2` + `v1.2.3` to GitHub.** **CRITICAL e-mail fix** (1.2.2): a merchant on WP Mail SMTP hit a fatal "critical error" on withdrawal confirmation + admin resend — an exception thrown inside `wp_mail()` by the SMTP plugin, or a Dompdf `\Error` on PHP 8, escaped WP's own `wp_mail()` catch (PHPMailer-only) and crashed the request though the withdrawal was already recorded; now `Mailer::send_html()` + the WC_Email route + the PDF block are `try/catch(\Throwable)` so a send failure degrades to the existing receipt_failed path, never a WSOD. + smoke fix: `withdrawal_request` fixtures used `order_id` instead of `order_ref`. + FluentCart **1.4.2** shipped its own native EU withdrawal add-on "customer rights" — our `native_addon_active()` auto-detect is still a placeholder, so **Settings → FluentCart** now warns about the two-flows risk + how to coexist (Off / `wwu_wb_fluentcart_native_active` filter); real auto-detection pending FluentCart's detection signal, asked the team; 1.2.1 fixes the WooCommerce My Account "Right of withdrawal" tab 404 on fresh activation via a one-time deferred rewrite flush + makes the legal clauses editable from Settings + new `wwu_wb_clause_text` dev filter; 1.2.0 added a merchant reminder to also update the shop's own legal texts — Terms & pre-contractual withdrawal article — per EU lawyer A. Vercellotti; Plugin Check clean + wordpress.org-ready; the `1.1.1` zip is the one in wp.org review, `1.2.0` ships after approval via SVN) (**configurable FluentCart handling** — Auto/Always/Off, Auto auto-defers to FluentCart's coming native add-on so no duplicate button; **UI Kit bundled** — Settings → Exemptions now renders styled, the kit was referenced but never packaged; + **Swedish (sv_SE)** complete UI translation (495/495) + statutory button labels from official EUR-Lex Art. 11a, pending native review — see [docs/analysis/wwu-wb-swedish-sv_SE-NOTE.md](docs/analysis/wwu-wb-swedish-sv_SE-NOTE.md). On top of alpha.39 critical Settings-page fatal hotfix + WooCommerce/conflict audit + `wp_mail_content_type` leak fix, alpha.38 subscription-aware withdrawal, the FluentCart `{{wwu.recesso_url}}` merge-tag, EDD parity, the full security audit). New workspace dev tools `wwu-tools/wwu-class-scan.php` + `wwu-phpstan.php` (out of repo/zip) catch the bare-class fatal family. **FluentCart handling is now configurable** (alpha.41): Settings → FluentCart → Auto/Always/Off, Auto auto-deferring to FluentCart's coming native add-on via filter `wwu_wb_fluentcart_native_active` so customers never see two buttons.
- **Target version:** `1.0.0` · **License:** GPL-3.0-or-later
- **Credits:** mredodos · Matteo Alfieri (An Idea for Business) · WebWakeUp ([webwakeup.it](https://webwakeup.it))
- **Product page (live):** [webwakeup.it/wwu-withdrawal-button](https://webwakeup.it/wwu-withdrawal-button/)
- **Legal go-live:** **2026-06-19** (contracts concluded on/after)
- **Last updated:** 2026-06-19 (**Legal Documents & Delivery feature — interviewed + dual recon + SPEC drafted (target `1.3.0`, awaiting confirmation before code).** User asked for: (1) opt-in injection of the withdrawal clauses into **Complianz** documents with solid generic-but-legally-sound defaults, (2) a generated consolidated **"Right of Withdrawal Policy"** document (Page + shortcode/block + PDF) assembled dynamically from the merchant's settings + selected Art. 59 exemptions, (3) a robust **translation workflow** so I stop producing malformed files. Interviewed via AskUserQuestion (Page+shortcode+PDF; Complianz both docs with per-document toggle; draft/preview + explicit publish + lawyer disclaimer; i18n tool = `wwu-tools/wwu-i18n.php` with wp-cli + `gettext/gettext`; MVP site-language; strengthened **dynamic** defaults). Dual recon (both sub-agents in parallel): the **local** code-scan agent died on autocompact-thrash (read a too-large file) so I did it by hand (ClauseLibrary 4 types + `wwu_wb_clauses`; `ExceptionTypes` 14 categories + `legal_ref` + tier + hint; `wwu_wb_settings`/`wwu_wb_exemptions[by_reason]` model; `Install::ensure_form_page` page pattern; `Shortcodes` 5 tags; `PdfBuilder`/`Template` reuse; `Compat/Complianz`); the **online** agent succeeded → [`docs/analysis/wwu-wb-complianz-i18n-law-recon-2026-06-19-ANALYSIS.md`](docs/analysis/wwu-wb-complianz-i18n-law-recon-2026-06-19-ANALYSIS.md) (659 lines): **key finding** `terms-conditions` is NOT native Complianz Premium — it needs the free `complianz-terms-conditions` companion (or self-register via `cmplz_pages_load_types` priority ≤5 with `document_elements => []`); element schema + `cmplz_flush_documents()`; EU/IT legal checklist (Art. 6 / 11a / 13–14 / 16 CRD, Art. 54-bis / 59 CdC, Annex I-A/I-B); `wp i18n make-mo` runs standalone + `gettext/gettext` v5 confirmed. SPEC written at [`docs/specs/wwu-wb-legal-documents-SPEC.md`](docs/specs/wwu-wb-legal-documents-SPEC.md) (12 sections). Prior **`1.2.6` complete the Italian translation — released.** A user reported the **Legal clauses** Settings section (and other recent admin strings) showing in English on an Italian site. Root cause: the `.pot` had not been regenerated since the 1.2.1 editable-clauses work, so strings added in 1.2.1–1.2.5 (the clauses editor, `Notification email(s)`, the Compliance legal-texts reminders, the FluentCart e-mail helper, `Reported reason: %s`, …) were `__()`-wrapped but absent from the catalogue → WordPress fell back to the English source. **Fix (translations only, no code):** regenerated `languages/wwu-withdrawal-button.pot` via `wwu-tools/wwu-generate-pot.php` (110 files, 562 unique strings); `msgmerge`d into `wwu-withdrawal-button-it_IT.po`; translated the **29** surfaced merchant-facing strings; recompiled `it_IT.mo` with Poedit's `msgfmt`. IT now **554 translated / 8 untranslated**, the 8 being intentional English (proper nouns + dev-tool labels: FluentCart, IP, WWU Withdrawal Button, Dashboard, Debug, Debug Inspector, Snapshot). A throwaway `_po_fix.php` applier (escaping + multiline + plural aware) did the bulk fill, removed after. Bumped 1.2.5 → 1.2.6. **Then completed the other 4 locales (same 1.2.6):** 4 parallel Sonnet sub-agents (de/es/fr/sv), each translating its locale's untranslated msgids and returning a JSON map applied by a gettext applier + recompiled `.mo`. Per-locale: de 544/5, es 546/3, fr 546/3, sv 543/6 (the remaining few are intentional English: FluentCart, IP, WWU Withdrawal Button, Dashboard, Debug, Snapshot). **Lesson:** the German agent went off-script — it re-extracted its own 19-item numbering (ignoring the supplied todo's 14) AND emitted malformed JSON (unescaped `"` inside values), so DE was rebuilt by hand from the agent's otherwise-good German; ES/FR/SV were verified aligned (todo n ↔ translation) before applying. A flaky Windows `/tmp` resolution (PHP `C:\tmp` vs bash `%LocalAppData%\Temp` vs the Write-tool path) also split the files across dirs — fixed by copying everything into a local `_potmp/` (removed after). **Swedish stays machine-assisted, pending Daniel's native review** (task #62). **Remaining follow-up:** the two **FluentCart Settings notices** (now translated in all 5 locales) still describe the pre-1.2.5 "no auto-detection yet, set Off manually" behaviour — now that 1.2.5 wired auto-detection, that copy (EN source + 5 locales) should be refreshed next. Prior **`1.2.5` PHP 7.4 fix (Dompdf 2.x) + multi-recipient notification e-mail — released.** Two community reports. **(1) Issue [#31](https://github.com/An-Idea-For-Business/wwu-withdrawal-button/issues/31)** — a merchant on PHP 7.4 saw *"Composer detected issues in your platform: Your Composer dependencies require a PHP version >= 8.1.0"* near the PDF-attachment option. Root cause: `composer.json` required `dompdf/dompdf: ^3.1` (resolved v3.1.5), whose tree (`sabberworm/php-css-parser` 9.x + `thecodingmachine/safe`) requires PHP 8.1 and Dompdf 3.x uses 8.1 syntax — contradicting our declared `Requires PHP: 7.4`. On a 7.4 site Composer's generated `vendor/composer/platform_check.php` (run when `vendor/autoload.php` loads to use Dompdf on the settings page) throws the platform error. Fix (the reporter's own suggestion): downgrade — pin `dompdf/dompdf` to `^2.0`, `composer update --with-all-dependencies` → **v2.0.8** (tree PHP 7.1+), dropping `thecodingmachine/safe` + the 3.x font/svg libs and downgrading css-parser to 8.9. `vendor/` is gitignored (built from `composer.lock` at package time by `bin/build.ps1 --no-dev`), so the commit is just `composer.json` + `composer.lock`; the regenerated `platform_check.php` now requires ≤ 7.4. The `PdfBuilder` API is identical in 2.x — verified by a standalone `Options/loadHtml/setPaper/render/output` run producing a valid `%PDF-1.7`. **(2) Facebook DM** — *"can the alert e-mails take more than one address separated by commas?"*: yes now. `Settings → Notification email(s)` is now a free-text field accepting a comma list; new `Sanitizer::email_list()` sanitises each via `sanitize_email()`, drops invalid/empty, de-dupes, caps at 10; `ConfirmationDispatcher` passes the whole list to `wp_mail()` (native comma `to`); `ReceiptBuilder` shows only the FIRST address as the public trader contact via new `Sanitizer::first_email()` so internal recipients never leak onto the consumer receipt. Single-address is fully back-compatible. 3 new smoke assertions in `suite_durable_medium`. Bumped 1.2.4 → 1.2.5, PHP lint clean. **Released [`v1.2.5`](https://github.com/An-Idea-For-Business/wwu-withdrawal-button/releases/tag/v1.2.5)** to GitHub. Replies drafted for issue #31 + the Facebook user. **Same 1.2.5 cycle, two more items:** (a) **FluentCart replied** (2026-06-19) with the stable detection signal for their free "Customer Rights" add-on (slug `fluent-cart-customer-rights`) — the constant `FLUENT_CART_CUSTOMER_RIGHTS_PLUGIN_PATH`, defined on `plugins_loaded:20` as their own double-load guard (won't be removed); also `class_exists('FluentCartCustomerRights')`. `FluentCartAdapter::native_addon_active()` (a placeholder returning `false` since the FluentCart coexistence work) now returns `defined(...) || class_exists(...)`, still wrapped in the `wwu_wb_fluentcart_native_active` filter → Auto mode genuinely auto-defers now, the two-flows risk is closed. FluentCart noted they don't yet expose a dedicated withdrawal-handling filter (maybe later). (b) **wp.org REJECTED the platform display name** — the directory forbids the restricted term **WooCommerce** in ANY plugin display name (not just the slug), with an explicit "do not work around by removing letters / using numbers". Edoardo's idea: a generic descriptor instead of named trademarks → renamed to **"WWU Right of Withdrawal for Popular Ecommerce Platforms"** (no trademark, conveys multi-platform; specific platforms named in the description + tags where allowed). Slug + text domain unchanged. The Dompdf downgrade is being verified by a Sonnet sub-agent audit (PdfBuilder API parity vs 2.x source + real receipt-template render + no orphaned `thecodingmachine/safe` / 3.x-lib references + platform_check ≤ 7.4). Prior **`1.2.4` WordPress.org pre-review hardening — released.** The wp.org plugin-directory bounced the submission to a pre-review (Review ID `TRM-OWN-LIC`): three "big" flags (trademark/name, ownership, license) + a technical checklist, most of which are false positives for us. Triage: **(1) Ownership** — the submission came from a `gmail.com` account while the plugin is authored by WebWakeUp (webwakeup.it), so they could not verify ownership; resolved by Edoardo changing the WP.org account email to `edoardo@webwakeup.it` (domain matches the brand → ownership proven, which also defuses the "WWU = unverified trademark" flag because WWU IS our brand). **(2) Name** — flagged as not distinctive; we refined only the DISPLAY name to **"WWU Right of Withdrawal"** while KEEPING the `wwu-withdrawal-button` slug, so the text domain + all six translations stay untouched (no i18n churn). **(3) Code fixes:** `sanitize_text_field( wp_unslash( $_SERVER['REMOTE_ADDR'] ) )` in `GuestAccess::check_rate_limit`; `esc_url()` instead of `esc_url_raw()` in the 3 plain-text-email URL outputs (single-param `?t=<40-hex>` receipt URLs, no `&`, so safe); explicit `permission_callback => '__return_true'` inlined on the 3 public `WithdrawalRoute` endpoints (was set via a shared `$args` the static scanner couldn't see) plus a comment that they are intentionally public (guest withdrawal, protected in-callback by nonce + per-IP rate limit + order-ref/email verification); removed `load_plugin_textdomain()` + its `init` hook (WP 4.6+ JIT-loads the bundled `/languages` `.mo`); readme **External services** expanded for the opt-in RFC 3161 / eIDAS authority (off by default, merchant-chosen provider — Sectigo/Aruba/InfoCert/… — whose own ToS + privacy apply). **(4) Explained to the reviewer, not defects:** the plugin is 100% free + fully functional (no trialware / locked features); the inline `<style>` blocks are only in standalone HTML responses (no-JS withdrawal flow + receipt-verification page) and the Dompdf PDF, where `wp_enqueue_*` does not apply; the 5 public REST endpoints are intentionally public with in-callback protection. Bumped 1.2.3 → 1.2.4; PHP lint clean. **Released [`v1.2.4`](https://github.com/An-Idea-For-Business/wwu-withdrawal-button/releases/tag/v1.2.4)** to GitHub + the same zip is what Edoardo re-uploads to wp.org. A short reviewer reply was drafted. Prior **`1.2.3` detailed mail-failure reason — released `v1.2.2`, `1.2.3` PR open.** Follow-up to 1.2.2: that release stopped the crash but still reported the failure generically (`wp_mail_returned_false` in the log + a fixed admin notice). 1.2.3 surfaces the SPECIFIC reason. `Mailer::send_html()` now hooks `wp_mail_failed` for the duration of the send to read WordPress's `WP_Error` message when `wp_mail()` returns false (the transport's own "Could not authenticate" / "Could not connect to host …"), and records `$e->getMessage()` when `wp_mail()` throws, both exposed via new `Mailer::last_error()` (trimmed + 300-char capped by `Mailer::cap()`). `ConfirmationDispatcher::dispatch()` captures `$fail_reason` from the WooCommerce `WC_Email` route's `catch` and then (most relevant) from `Mailer::last_error()` when the standalone send failed, and writes it into BOTH the immutable `receipt_failed` log payload AND the `wwu_wb_mail_failed` transient (now `array{uid,reason}` instead of a bare uid). `AdminController::maybe_mail_failure_notice()` reads the transient — back-compatible with a bare-string uid from a 1.2.2-era failure — and appends "**Reported reason: …**" to the admin notice. So an SMTP misconfiguration (WP Mail SMTP / FluentSMTP / provider mailer) is diagnosable at a glance, no PHP-error-log digging; legal guarantee unchanged (withdrawal always recorded, consumer always reaches the confirmation page, failure always logged + resendable). 3 files, PHP lint clean; no schema change, the transient shape change is back-compatible. **Released [`v1.2.2`](https://github.com/An-Idea-For-Business/wwu-withdrawal-button/releases/tag/v1.2.2) to GitHub (Latest)** — the merged 1.2.2 critical fix; verified the zip carried the `Mailer` `try/catch` + `Stable tag: 1.2.2` before publishing. **`v1.2.3` PR open** off `main` for merge → release. Prior **`1.2.2` CRITICAL e-mail-send fix + FluentCart coexistence + smoke fix.** A merchant running WP Mail SMTP (free) across several WooCommerce sites reported a fatal "critical error" on withdrawal confirmation AND on the admin "Resend e-mail" action. Root cause: an exception raised INSIDE `wp_mail()` by an SMTP plugin (WP Mail SMTP / FluentSMTP / provider mailer), or a `\Error` from Dompdf on PHP 8, escaped — WordPress's own `wp_mail()` only catches `\PHPMailer\PHPMailer\Exception`, so any other `\Throwable` propagated up through `do_action('wwu_wb_withdrawal_confirmed')` to the confirm handler and produced a WSOD, even though the withdrawal was already recorded. Fix: the send path is exception-safe end to end — `Mailer::send_html()` wraps `wp_mail()` in `try/catch(\Throwable)` (returns false + logs `durable_medium:mail.exception` + error_log); the WooCommerce `WC_Email` route is wrapped and falls through to the standalone mailer on a throw; the optional PDF block is wrapped (skips the attachment on a Dompdf error). A send failure degrades to the existing `receipt_failed` path (admin notice + resend), never a fatal; the consumer always reaches the confirmation page. **Smoke fix:** the `withdrawal_request` suite built fixtures with `order_id` (ignored by `from_input()`, which reads `order_ref`), so `is_valid_unaffected` went red on an empty `order_ref` — the four fixtures now use `order_ref`; no plugin-code change, `is_valid()` was always correct (live flow passes `order_ref`). Bundled into the same `1.2.2` (PR #29, still open) as the **FluentCart native add-on coexistence** — FluentCart **1.4.2** (June 2026) shipped its first-party EU withdrawal add-on **"FluentCart customer rights"** (public no-login page, two-step timestamped flow, item/qty selection + tax-aware refund estimate, subscription withdrawal, duplicate guard, admin Withdrawal Requests page, automated e-mails) — the native feature they'd flagged 2026-06-15, the overlap our `fluentcart_mode` Auto-defer anticipated. **Gap:** `FluentCartAdapter::native_addon_active()` is still a placeholder returning `false` (no published detection signal when written), so Auto mode does NOT yet auto-defer → a store running both could show two withdrawal flows. **Interim fix (no fragile guessing):** the **Settings → FluentCart** copy is corrected — the old "*if* FluentCart *later* ships an add-on, Auto steps aside automatically" notice (now wrong) becomes a `notice-warning` stating the add-on exists (FluentCart 1.4.2+), that auto-detect isn't wired yet, and what to do (set **Off**, or return true from `wwu_wb_fluentcart_native_active`) so customers don't see two flows; "Show example" updated likewise. **Next:** asked the FluentCart team for the canonical detection signal (class/constant/plugin slug) → then `native_addon_active()` gets a one-line real check so Auto truly auto-defers. WooCommerce + EDD unaffected. README status badge → 1.2.2. Strategic posture stays **detect-and-coexist** on FluentCart (don't compete); differentiation = WooCommerce + EDD + evidence/timestamp/multi-platform. [[feedback_consult_official_docs]] Prior **`1.2.1` bug fix** — a developer (Antonio Costa) reported the WooCommerce My Account "Right of withdrawal" tab 404ing on a fresh install. Root cause (broad: every fresh activation): the tab is a rewrite endpoint registered by `WooMyAccount` on `init`, but the activation-time `flush_rewrite_rules()` runs *before* that (the plugin boot never runs during activation — `plugins_loaded` already fired), so the endpoint rule is never persisted → 404 until permalinks are re-saved. Fix: a one-time **deferred flush** — `Install::setup_site()` sets an autoloaded `wwu_wb_flush_pending='1'`; new `Install::maybe_deferred_flush()` on `wp_loaded` flushes once (after `init` registered the endpoint) and flips the flag to `'0'` (cache-only no-op after). Wired in the bootstrap; option added to uninstall cleanup. The slug is a **WC endpoint, not a page** — no page to create; re-saving Permalinks is the manual workaround. Also — answering Antonio's other question ("can I edit the clause texts?") — the legal clauses are now **editable from the admin, no code**: a new **Settings → Legal clauses** section (`SettingsPage::render_clauses_section`) with an editable textarea per type for the current admin language + a "show built-in default" toggle, saved to the new autoload:no option `wwu_wb_clauses` (`[type][lang]`, empty reverts to template). `ClauseLibrary::get()` returns a saved override **verbatim** (no sample-text disclaimer / `[EN]` prefix) and short-circuits the template path; the Compliance page shows a "customised" badge + a pointer to the section. New `ClauseLibrary` API: `OPTION` / `has_override()` / `default_text()`. Developers can still override via the **`wwu_wb_clause_text`** filter (both override + default paths). Option added to uninstall cleanup. Hooks reference 33→34. No DB change. PHP lint clean. Reply drafted for Edoardo to send Antonio. Prior **`1.2.0` "update your legal texts too" merchant reminder** — prompted by EU consumer lawyer Alessandro Vercellotti's public note that the button is mandatory but the Terms & Conditions of sale must **also** be amended in the withdrawal article to provide for the new button modality (Art. 6 CRD: the trader must inform the consumer HOW to withdraw, now including the online button). Installing the plugin adds the button but cannot edit the merchant's own published documents, so: `DashboardPage` gains a prominent "Installing the button is not enough — update your legal texts too" card linking to Compliance; `ComplianceStatusPage` gets a `notice-warning` callout and now renders the two clauses to paste (pre-contractual information + general terms) **open by default**; `ClauseLibrary`'s `terms` "How to withdraw" clause now **names the button explicitly** ("l'apposito pulsante di recesso online — 'Recedere dal contratto qui'" / "the dedicated online withdrawal button") IT+EN. Marketing landing + docs pages synced. No change to the withdrawal flow, storage or evidence; PHP lint clean; smoke tests assert clause presence not wording. The `1.1.1` zip remains the one in the wordpress.org manual-review queue; `1.2.0` ships to the directory via SVN after approval. Prior **`1.1.1` Plugin Check polish** — removed the unused UI-kit `clipboard.js` (filename collided with a WP-core lib; never enqueued — only accordion/badge/utilities are) from repo + package + loader, and moved the readme "Product page" line out of the short-description block (it was being counted toward the 150-char limit). The remaining `DirectDatabaseQuery`/`UnescapedDBParameter` notices are advisory false positives (table name = `$wpdb->prefix` + constant). `v1.1.0` was published on GitHub (Latest) with the zip. Prior **`1.1.0` evidence-log hardening** — closes the 3 deeper audit items before the wp.org submission: **HIGH-1** the hash chain is now HMAC-keyed with the site secret (`LogChain` v2, per-row `chain_version`, schema 2→3 + `Migration_3`) so a DB-write attacker without the secret can't forge it; **MEDIUM-1** the hash commits to the anonymised IP while the full IP lives in a new purgeable `ip_full` column blanked by the retention sweep (GDPR); **HIGH-2** RFC 3161 now requires HTTPS + binds the token to our exact digest/nonce, and failed OpenTimestamps/initial stamps are retried on cron with un-anchored rows surfaced in admin. Existing logs keep verifying; no withdrawal-flow change. 5 new smoke tests. See `docs/audits/wwu-wb-full-2026-06-17-AUDIT.md`. Prior **`1.0.1` wordpress.org readiness + audit hardening** — Plugin Check fixes (unused `clipboard.js` excluded from build, `composer.json` shipped with `vendor/`, SSRF smoke-test `localhost`→private IP, Tested-up-to 7.0) + low-risk fixes from a full 5-part security audit (OpenTimestamps SSRF-guard parity + `redirection=0`, `wp_unslash` on 2 credential fields, defensive `return`s). Audit verdict **0 exploitable XSS/SQLi/SSRF/CSRF/IDOR**; 3 deeper integrity/privacy items deferred to a follow-up — unkeyed hash chain, incomplete timestamp verification, withdrawal-event IP retention — see `docs/audits/wwu-wb-full-2026-06-17-AUDIT.md`. Prior **`1.0.0` — first stable release** — promotes the alpha series to stable for the 19 June 2026 EU mandate; reconciled translations with the Crowdin TMS after PR #20 via a gettext union (all 6 locales 545/545, `.mo` recompiled) and clarified the External-services disclosure (optional RFC 3161 + outbound webhook) for the wordpress.org submission; no functional change from alpha.45. Prior **`1.0.0-alpha.45` FluentCart e-mail link helper + verified email-reachability matrix** — withdrawal link is auto-added to WooCommerce customer order e-mails (`woocommerce_email_after_order_table`) + EDD purchase-receipt e-mails (`edd_order_receipt`/`edd_purchase_receipt`); FluentCart exposes no auto-append hook (re-verified vs current dev docs, June 2026), so Settings → FluentCart now shows an optional one-time merge-tag setup helper (`{{wwu.recesso_url}}` + 3 steps), gated on active+handled. Recital 37 "hyperlinks to the withdrawal function" satisfied across all 3 platforms within each platform's documented API. 7 new admin strings, all 6 locales 545/545; `php -l` clean. Prior **`1.0.0-alpha.44` automations** — read-only REST API (`wwu-wb/v1`, Application-Password auth, `/requests`, `/requests/{uid}`, `/orders/{platform}/{ref}/withdrawal`) + signed outbound webhook (HMAC-SHA256 on `wwu_wb_withdrawal_confirmed`, dual SSRF check, secret autoload-off/masked) under Settings → Integrations; PII-first (no raw IP, only `row_hash`); no create endpoint by design; new `Api\{RequestReader,Webhook,WebhookDispatcher}` + `REST\Routes\ApiRoutes` + option `wwu_wb_webhook`; dedicated Opus security audit verdict SHIP (0 crit/high/med); REST API reference + 33 hooks; smoke suite `automations`; PHPStan L2 clean. Prior **`1.0.0-alpha.43` consumer "why exempt" transparency note** — when an order is exempt under Art. 59, a short note on the order surfaces (form/block, Woo account detail, FluentCart portal, EDD) names the statutory exception + legal ref instead of silence; new `ExemptionNoteRenderer`, strictly gated on `no_withdrawal_right` + fail-safe, merchant-overridable (`custom_exemption_note`) + filter `wwu_wb_exemption_note_text`; button visibility unchanged; all 6 locales 517/517; PHPStan L2 + class-scan clean. Prior **`1.0.0-alpha.42` optional partial-withdrawal product selection** — step 1 of the withdrawal form gains an optional per-item checklist (`statement.products`, mirrors the `reason` field, fail-open: empty = whole order), flowing to the immutable evidence log + durable-medium receipt (e-mail + PDF) + a new Requests "Products" column; informational only (refund stays manual); all 6 locales 510/510; PHPStan L2 + class-scan clean; 5 new smoke assertions. Prior **`1.0.0-alpha.41` configurable FluentCart handling** — new Settings → FluentCart mode `auto|always|off` (default auto) gating only the 4 consumer FluentCart surfaces via `FluentCartAdapter::should_render()`; admin Requests + in-flight confirmation untouched; native-add-on detection filterable (`wwu_wb_fluentcart_native_active`); 8 new smoke assertions, PHPStan L2 + class-scan clean. Prior **`1.0.0-alpha.40`**: UI Kit bundled (Settings → Exemptions styled) + Swedish sv_SE complete (495/495). **`1.0.0-alpha.39` critical hotfix** — `SettingsPage::render_exemptions_status()` called `Settings::main()` without `use WWU\WithdrawalButton\Core\Settings;`, so PHP resolved it to the non-existent `…\Admin\Settings` and fataled the whole Settings page in production. Fixed; a token scanner over all 91 `src/` files confirmed it was the only bare-class fatal. Bundled with the WooCommerce/conflict audit + `Mailer` `wp_mail_content_type` try/finally fix. Prior: **Subscription-aware withdrawal — `1.0.0-alpha.38`**: per the SPEC, the button now shows on the initial order only and is suppressed on renewals via one `renewal_order` gate fed by `NormalizedOrder::$is_renewal`; new `SubscriptionAware` interface on all 3 adapters; two opt-in Settings → Subscriptions toggles; Requests "Subscription" badge + reminder; `confirm()` stamps the link + optional auto-cancel + `subscription_cancelled` evidence. Needs a live test with a subscription plugin. On top of alpha.37 FluentCart merge-tag.)

## What it is (one paragraph)
A free, open-source WordPress plugin that makes a store compliant with Directive (EU) 2023/2673 (new Art. 11a of the Consumer Rights Directive 2011/83/EU; Italy: Art. 54-bis Codice del Consumo via D.Lgs. 209/2025): a prominently displayed, continuously available, statutory-labelled withdrawal button → two-step statement + confirmation → durable-medium acknowledgement (email + PDF + verifiable link) → tamper-evident immutable log anchored to OpenTimestamps. Dual platform (WooCommerce HPOS+legacy / FluentCart), multilingual (IT/EN/FR/ES/DE + extensible), Complianz/TranslatePress-compatible, shortcodes + blocks, plus generators for the Annex I-B model form and Privacy/Terms/pre-contractual clauses.

## Conventions
Namespace `WWU\WithdrawalButton` · constants `WWU_WB_*` · options `wwu_wb_*` · meta `_wwu_wb_*` · REST `wwu-wb/v1` · hooks `wwu_wb_*` · CSS `.wwu-wb-*` · text domain `wwu-withdrawal-button` · JS `window.wwuWbData`.

## Specifications
- [SPEC — EU withdrawal button](docs/specs/wwu-wb-eu-withdrawal-button-SPEC.md) — 12 canonical sections; the authoritative design (2026-06-13).
- [SPEC — Legal Documents & Delivery (Policy doc + Complianz injection + dynamic defaults + i18n tooling)](docs/specs/wwu-wb-legal-documents-SPEC.md) — **DRAFT, target `1.3.0`** (2026-06-19): a dynamic `PolicyBuilder` assembling a consolidated "Right of Withdrawal Policy" from live settings + the selected Art. 59 exemptions, delivered as an auto-created **draft Page + `[wwu_wb_policy]` shortcode/block + downloadable PDF**; **opt-in Complianz injection** into Terms & Conditions (gated on the `complianz-terms-conditions` companion add-on) + Privacy Statement via the `cmplz_document_elements` filter; strengthened **dynamic** default clauses; and a reusable **`wwu-tools/wwu-i18n.php`** pipeline (wp-cli + `gettext/gettext` v5 + `msgfmt --check`). Draft/preview + explicit publish + lawyer-review disclaimer throughout. Grounded in the dual recon: in-session local scan + [`docs/analysis/wwu-wb-complianz-i18n-law-recon-2026-06-19-ANALYSIS.md`](docs/analysis/wwu-wb-complianz-i18n-law-recon-2026-06-19-ANALYSIS.md).
- [SPEC — Withdrawal exemptions (Art. 59)](docs/specs/wwu-wb-withdrawal-exemptions-SPEC.md) — exempting products/services with the legal consent-capture conditions; exception-type registry + admin UI + **P1–P3 + FluentCart shipped** (alpha.27→alpha.30: tagging, WooCommerce **classic + block** + FluentCart checkout consent capture, durable-medium confirmation, retention/purge, GDPR clause, consent-records view, grouped/tooltip'd UX, full i18n). All capture surfaces shipped; EDD is the next platform (SPEC) (2026-06-14).
- [SPEC — EDD (Easy Digital Downloads) integration](docs/specs/wwu-wb-edd-integration-SPEC.md) — **shipped alpha.33**: third platform adapter (EDD 3.0+) `EddAdapter` + `EddCheckoutConsent`, category-aware exemptions, on the official-source-verified surface (2026-06-14). Live EDD test pending.
- [SPEC — Exemptions management UX](docs/specs/wwu-wb-exemptions-ux-SPEC.md) — **shipped alpha.31**: grouped + tooltip'd exemptions settings (WWU UI Kit accordions), a guided "what do you sell?" helper, consumer-preview of the checkbox + durable-medium e-mail, an exemptions status/health panel, full it/fr/es/de i18n, and a landing section with worked examples (events/recordings/Zoom). Core button untouched (2026-06-14).
- [SPEC — North America subscription-cancellation ("click-to-cancel")](docs/specs/wwu-wb-subscription-cancellation-na-SPEC.md) — design (no code yet) for a DISTINCT click-to-cancel module in the same plugin, jurisdiction-toggled (Quebec Bill 10, BC Bill 4, Ontario/NB pending, US ROSCA + state ARLs incl. California AB 2863), with WooCommerce Subscriptions + FluentCart integration. Grounded in a 5-agent official-source sweep (2026-06-14).
- [SPEC — Subscription-aware withdrawal (EU 14-day)](docs/specs/wwu-wb-subscriptions-withdrawal-SPEC.md) — **shipped alpha.38** (2026-06-15): the withdrawal button is now subscription-aware — button on the INITIAL order only, **suppressed on renewals** (one `renewal_order` gate in `ApplicabilityResolver` fed by `NormalizedOrder::$is_renewal`, auto-covering all 8 surfaces; + a `Shortcodes::form()` guard). Per-platform renewal detection (WC Subscriptions `wcs_order_contains_renewal`; FluentCart `Subscription.parent_order_id`; EDD Recurring `edd_subscription` status) via the optional `SubscriptionAware` adapter interface (fail-open); optional auto-cancel-on-withdrawal (default off, `confirm()` stamps the link + `subscription_cancelled` evidence) + Requests "Subscription" badge & reminder (cancel + Art. 14(3) pro-rata, all manual). Two opt-in Settings → Subscriptions toggles. Filter `wwu_wb_order_is_renewal`. Live test pending. Distinct from the NA click-to-cancel module.
- [SPEC — Configurable FluentCart handling (Auto/Always/Off)](docs/specs/wwu-wb-fluentcart-handling-mode-SPEC.md) — **shipped alpha.41** (2026-06-15): a Settings → FluentCart mode (`wwu_wb_settings['fluentcart_mode']`, default `auto`) gating our consumer FluentCart surfaces, with Auto auto-deferring to FluentCart's coming native add-on (`FluentCartAdapter::should_render()` + filter `wwu_wb_fluentcart_native_active`). Gates the 4 consumer entry points only; admin Requests dashboard + in-flight durable-medium confirmation untouched (no stranded requests).
- [SPEC — Optional "which products" field (partial withdrawal)](docs/specs/wwu-wb-partial-withdrawal-products-SPEC.md) — **shipped alpha.42** (2026-06-16): an optional per-item checklist in step 1 of the withdrawal form lets the consumer indicate which products they withdraw from (EU partial withdrawal — Annex I-B "the following goods"). Mirrors the `reason` field (`statement.products`), fail-open (empty = whole order, `is_valid()` unchanged), informational only (refund stays manual). Flows to the evidence log + durable-medium receipt + a new Requests "Products" column.

- [SPEC — Consumer "why exempt" transparency note](docs/specs/wwu-wb-exemption-why-note-SPEC.md) — **shipped alpha.43** (2026-06-16): when an order is exempt under Art. 59, the consumer sees a short note explaining the specific exception + legal ref (instead of silence). `ExemptionNoteRenderer` wired into the form/block, Woo account detail, FluentCart portal and EDD surfaces. Strictly gated (`no_withdrawal_right` only) + fail-safe (empty → nothing); merchant-overridable (`custom_exemption_note`) + filter `wwu_wb_exemption_note_text`. Button visibility unchanged.

## Developer reference
- [Hooks & filters reference](docs/reference/wwu-wb-hooks-filters-REFERENCE.md) — the supported public extension API: **33 hooks** (27 filters + 6 actions), each with signature, parameter types, purpose and a usage example, grouped (access control · applicability & eligibility · platforms/adapters · FluentCart · exemptions Art. 59 · rendering/surfaces · guest access & security · durable medium/evidence · timestamping · lifecycle/log · automations) + an at-a-glance table. Hooks not listed are internal and may change. (updated 2026-06-16, alpha.44)
- [REST API & webhook reference](docs/reference/wwu-wb-rest-api-REFERENCE.md) — **shipped alpha.44**: the read-only automations API (`wwu-wb/v1`, Application-Password auth) + the signed outbound webhook (HMAC-SHA256). Endpoints, response shapes, payload, signature-verification examples (PHP + Node), security notes. PII-first (no raw IP). See also the [SPEC](docs/specs/wwu-wb-rest-api-automations-SPEC.md). (2026-06-16)

## Localization
- [Crowdin localization workflow](docs/reference/wwu-wb-localization-crowdin-REFERENCE.md) — professional TMS pipeline (2026-06-16): `crowdin.yml` mapping + **Crowdin native GitHub App** (no workflow files — the repo's `gh` OAuth push token lacks the `workflow` scope, empirically confirmed, so `.github/workflows/` stays excluded; the App authorizes itself independently and opens translation PRs). Native reviewers (e.g. Daniel for Swedish, **Proofreader** role) work in the Crowdin web UI with translation memory + glossary + QA — replacing the hand-fill + CSV proofread. `.pot` regenerated locally (WP-CLI `make-pot` canonical; `wwu-tools/wwu-generate-pot.php` = convenience); `.mo` recompiled locally on each sync PR. A fully-automated GitHub Actions variant is documented as an alternative (needs a one-time `gh auth refresh -s workflow`). Future: translate.wordpress.org / GlotPress once on the .org repo.

## Legal reference
- [Legal reference (verbatim)](docs/legal/wwu-wb-legal-reference.md) — Art. 11a EN+IT, Recital 37, Art. 54-bis, Annex I-B, per-country labels (DE §356a / FR D.221-5 / ES / CH), Rome I applicability, GDPR.
- [Compliance matrix](docs/legal/wwu-wb-compliance-matrix.md) — clause → feature → test, with the Standard #14 acceptance gate.
- [Exemption-consent evidence & record-keeping](docs/legal/wwu-wb-exemption-consent-evidence-NOTE.md) — official-source + adversarially-verified note (2026-06-14): no named "register" (burden of proof Art. 6(9) CRD + GDPR accountability Art. 5(2)); durable-medium confirmation **constitutive** for the digital exemption (Art. 59(1)(o) CdC / Art. 16(1)(m)+14(4)(b)(iii) CRD); 10-year defensible retention (art. 2946 c.c.) configurable + purge routine; GDPR lawful basis = legitimate interest (Art. 6(1)(f)), not consent. Drives exemptions P3 + the privacy clause.

## Plans
- [Implementation roadmap (PLAN)](docs/plans/wwu-wb-roadmap-PLAN.md) — phases F0–F9 + audits + queued HyperFrames video + post-MVP.

## Audits
- [Core F0–F6 audit (2026-06-13)](docs/audits/wwu-wb-core-2026-06-13-AUDIT.md) — security (0 findings) + performance + compliance; all critical/high gaps closed.
- [Full-plugin security audit (2026-06-15)](docs/audits/wwu-wb-full-security-2026-06-15-AUDIT.md) — whole-codebase, 10 dimensions, multi-agent + adversarial verification. **0 critical / 0 high.** 7 dimensions clean (SQLi/XSS/CSRF/IDOR/files/crypto/supply-chain); fixed 1 Medium SSRF (RFC 3161 endpoint → new `Security\OutboundUrlGuard`) + a Low cluster (rate limits, input caps, debug masking, uninstall cron) in alpha.36.
- [WooCommerce surface + plugin-conflict audit (2026-06-15)](docs/audits/wwu-wb-woocommerce-2026-06-15-AUDIT.md) — inline file-by-file (the multi-agent run was blocked 3× by an Anthropic server-side rate limit). **0 critical / 0 high.** HPOS purity, consent capture (classic + block), refund recorder, My Account (no IDOR) and the conflict surface (namespaced handles/shortcodes/admin-post/block/REST/options/meta; scoped `ob_start`; handle-scoped `script_loader_tag`; activation-only rewrite flush) all clean. Fixed 1 Low conflict-safety bug (`Mailer::send_html` now removes the global `wp_mail_content_type` filter in a `finally`). Documented 3 items for the live test: order-status transition firing other plugins' hooks (by design), the button rendering on the thank-you page, and re-verifying the block-checkout Store API field shape/meta key (fail-safe either way).

## Analysis
- [Timestamp providers (RFC 3161 + eIDAS)](docs/analysis/wwu-wb-timestamp-providers-ANALYSIS.md) — which trusted-timestamp authorities to add to the pluggable provider (free Sectigo `/qualified`, per-country QTSPs) + PHP integration (2026-06-14).
- [EDD customer surfaces (verified)](docs/analysis/wwu-wb-edd-customer-surfaces-ANALYSIS.md) — the EDD 3.x hooks (source-verified) that render the withdrawal button on the EDD receipt / purchase history / receipt e-mail; documents that the legacy 2.x `edd_payment_receipt_*` hooks were removed from 3.x templates (alpha.35, 2026-06-15).
- [FluentCart hooks (verified)](docs/analysis/wwu-wb-fluentcart-hooks-ANALYSIS.md) — official-source verification of every FluentCart hook/API (custom_endpoints, menu items, dashboard data, order-details slots, Order/Customer models) + the corrections shipped in alpha.19; **+ §"Second verification round" (2026-06-15)** = a direct FluentCart-team reply confirming custom-field submission, the `before_payment_methods` render hook, block-checkout flow, the `product-categories` taxonomy, `getViewUrl('admin')`, `order_paid`, `smartcode_fallback`, `fluent_cart_add_log` — all actioned in alpha.34.
- [Complianz integration + EU/IT law + i18n toolchain recon (2026-06-19)](docs/analysis/wwu-wb-complianz-i18n-law-recon-2026-06-19-ANALYSIS.md) — synthesis report grounding future SPEC work: (1) complete `cmplz_document_elements` element schema (all keys, 11 gotchas, PHP code example adding withdrawal clauses, `cmplz_pages_load_types` registration for the `terms-conditions` type which lives in the separate `complianz-terms-conditions` companion plugin — **C1 CONTRADICTED**); (2) EU/IT legislative framework — Dir. 2011/83/EU, new Art. 11a (Dir. 2023/2673, in force today), Italian Art. 54-bis Codice del Consumo (D.Lgs. 209/2025), durable-medium analysis, Art. 16 exceptions, 6-domain compliance checklist F.1–F.6, AGCM €9M enforcement context; (3) WP-CLI `wp i18n` 5 subcommands (all standalone, no WP install needed — **C3 CONFIRMED**), critical pipeline ordering (`make-mo` before `make-json`), and `gettext/gettext` PHP library v5 API (PoLoader/MoLoader/PoGenerator/MoGenerator/Translations/Translation).

## Testing ([docs/testing/](docs/testing/README.md) — index)
**End-to-end "try the plugin" evaluator guides** (install → button/entry points → two-step flow + no-JS → durable medium e-mail/PDF/verify link → evidence-log integrity → merchant refund + processing → exemptions → uninstall):
- [Try the plugin — WooCommerce](docs/testing/wwu-wb-try-the-plugin-woocommerce-CHECKLIST.md) — 3 button surfaces (My Account orders action / order detail / "Right of withdrawal" tab).
- [Try the plugin — FluentCart](docs/testing/wwu-wb-try-the-plugin-fluentcart-CHECKLIST.md) — 4 portal surfaces (endpoint / sidebar / dashboard banner / per-order button).
- [Try the plugin — EDD](docs/testing/wwu-wb-try-the-plugin-edd-CHECKLIST.md) — 3 surfaces since alpha.35 (purchase receipt / purchase-history rows / receipt e-mail link) + standalone page / payment-key / guest lookup.

**Exemptions consent-capture (Art. 59) focused checklists:**
- [WooCommerce **block** Checkout consent](docs/testing/wwu-wb-woocommerce-block-consent-CHECKLIST.md) — Additional Checkout Fields API (WC 9.9+).
- [**FluentCart** Checkout consent](docs/testing/wwu-wb-fluentcart-consent-CHECKLIST.md) — `before_payment_methods`, category-aware, activity-log note.
- [**EDD** Checkout consent](docs/testing/wwu-wb-edd-consent-CHECKLIST.md) — `edd_purchase_form_before_submit` + `edd_built_order`, `download_category`-aware.
- All are runnable on a staging store by anyone; everything is fail-safe (button stays whenever a surface/capture is unavailable).

## Changelog
- [CHANGELOG](docs/changelog/wwu-wb-CHANGELOG.md)

## Key decisions (quick reference)
| Decision | Choice | Why |
|---|---|---|
| Platforms | WooCommerce (HPOS+legacy) + FluentCart, Day 1, adapter | user requirement |
| PDF library | **Dompdf** (LGPL-2.1) | GPLv3-compatible; mPDF GPL-2.0-only avoided |
| Timestamping | **OpenTimestamps** (free, Bitcoin) + pluggable RFC 3161/eIDAS | free *data certa* now, eIDAS later |
| Log storage | 2 custom tables, append-only **hash chain**, DATETIME | tamper-evidence; Options API can't serve it |
| Applicability | per **consumer country** (Rome I Art. 6); CH = voluntary | follows the consumer, not the seller |
| Labels | statutory defaults per country/locale; override warns | Art. 11a "only words" for confirmation |
| Guest access | signed order-email link + public lookup + receipt to titleholder + rate-limit | coverage without dark-pattern friction |
| MU-plugin | **none** (only multisite-aware activation) | no need to filter active_plugins |
| Distribution | GitHub public now, wordpress.org later | 6-day deadline; review is slow |
